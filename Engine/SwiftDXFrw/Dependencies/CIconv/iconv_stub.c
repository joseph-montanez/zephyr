// =========================================================================
// MARK: - CIconv Stub
//
// SPM requires at least one C source file per Clang target.
// This empty file is compiled (producing no code) so that the module
// map and headers are properly processed by the build system.
// All actual iconv functionality comes from the shared library
// (iconv-2.dll / iconv.lib) linked via Package.swift linkerSettings.
// =========================================================================

// Intentionally empty — the module map and linker settings are what matter.
